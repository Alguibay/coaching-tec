{
  "name": "rafael-dev-plan",
  "version": "1.0.0",
  "description": "Plan de Desarrollo — Rafael",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required for static files' && exit 0"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
